## What I did today

- Filed the public pre-registration as a GitHub gist
  (https://gist.github.com/spayyavula/2c42dceaf372a839ddc1fb7ceb9e4a04)
  and announced it on LinkedIn.
- Encoded the locked spec from `pre-registration.md` §§3–13 as a frozen
  dataclass in `src/locked_spec.py`, with a `fingerprint()` over a
  canonical JSON serialization.
- Added `tests/test_locked_spec.py` pinning the filing-time fingerprint;
  the test fails loudly if anyone edits the spec without updating the pin
  and adding a decision-log entry.
- Built the §3 acquisition layer: `src/storage.py` (date→split,
  partitioned parquet, MANIFEST updater), `src/news.py`,
  `src/options.py`, `src/underlying.py`, `src/risk_free.py`, and the
  `src/data_acquisition.py` CLI dispatcher.
- Verified on PyPI that `polygon-api-client` is still active (1.16.3,
  Oct 2025) and still targets polygon.io. Bumped `requirements.txt` to
  match and added `requests==2.32.3`.
- Added `tests/test_storage_split.py` covering split boundaries,
  contiguity (no silent gap between train/validation/holdout), and
  outside-the-window failure.
- Smoke-tested FRED end-to-end (no auth): pulled 15 real observations
  of DGS3MO for May 2026 (3.65–3.70 %).

## Blockers / risks

- Two acquisition questions deferred and called out in
  `src/options.py`: ingest-time strike window (Q1) and mid-quote source
  (Q2). Both blocked on empirical answers from the vendor; settled in
  Week 2.
- Local Python is 3.13; `requirements.txt` targets 3.11 because
  `torch==2.4.1` does not ship 3.13 wheels. Acquisition layer itself
  runs fine on 3.13; FinBERT inference (Week 3) will need a 3.11 venv.

- Built the §§6-9 eval harness: `src/metrics.py` (Spearman IC,
  bootstrap CI, hit-rate, Sharpe-approx), `src/baselines.py` (B1
  persistence, B2 random, B3 prior-day-same-time), and
  `src/evaluate.py` (`evaluate_primary` with sub-period stability and
  explicit S1-S4 pass/fail + grey-zone classification). All thresholds
  read from `SPEC`.
- Added 21 unit-test cases across `test_metrics.py`, `test_baselines.py`,
  `test_evaluate.py`. They skip cleanly when numpy/pandas/scipy aren't
  installed yet; the 7 pure-Python tests (locked-spec + storage-split)
  pass on the bare checkout.
- Built a resumable laptop-runtime orchestrator: `src/orchestrator.py`
  with a JSON state file in `.phase0/state.json`, 9 registered stages
  (4 real backfill stages + 5 explicit stubs for future modules), and
  a `run|status|pause|resume|reset|stages` CLI. Lazy imports keep the
  CLI working before `pip install`.
- Wrote `scripts/install-orchestrator.ps1` + uninstall to register the
  runner as a Windows Scheduled Task: starts 30 s after logon, restarts
  on failure, ignores additional launches if one is already running,
  ignores AC-power state. State and logs land in `.phase0/`.

- Installed Python 3.11.9 via winget (3.13 stays in place) and built
  `.venv` against the pinned `requirements.txt`. Verified the
  Massive/Polygon key end-to-end through three live endpoints
  (`get_market_status`, `get_previous_close_agg`,
  `list_options_contracts`).
- Resolved Q1 (ingest strike window) and Q2 (mid-quote source) in
  `src/options.py` with empirical Polygon probes — 602 SPY daily bars
  for the intraday-excursion distribution and a NBBO tick-density
  probe on `O:SPY260522C00741000`. Locked the ingest window at
  ATM-open ± 25 strikes (covers ~99.3 % of trading days for the locked
  ATM-event ± 2 sample) and chose path (b) — windowed `list_quotes`
  with as-of `sip_timestamp <= sample_time` semantics — for mid-quote
  pulls. Per-minute *quote* aggregates do not exist in the REST API
  and `get_snapshot_option` is live-only, so (b) is the only viable
  historical-backfill path. Added `list_contracts_for_ingest` and
  `fetch_option_mid_quote_at` to `src/options.py`; the Q1/Q2
  deferred-docstring blocks are gone and the orchestrator's
  `backfill_options` stub message now points at the new decision-log
  entry.
- Pulled January 2024 across all three live sources to shake out
  vendor quirks before unattended backfill: 22 risk-free obs, 16 639
  SPY 1-minute bars (plus 50 spillover into `2024-02.parquet` because
  Jan 31 ET after-hours bars timestamp into Feb 1 UTC — the storage
  layer's per-row date→split routing handled it correctly), and 2 219
  unique news articles after the 11-ticker fanout dedupe.
- Added `load_dotenv()` to `src/orchestrator.py` so the Scheduled Task
  picks up `MASSIVE_API_KEY` from a fresh login context, then
  installed and started the task with
  `-PythonPath .\.venv\Scripts\python.exe`. First 50 s chewed through
  four months of risk-free backfill (2024-01 → 2024-04) at the
  expected ~16 s/month cadence (15 s `RATE_LIMIT_SLEEP_SECONDS` + ~1 s
  per FRED call).
- One hour later: `backfill_risk_free` complete in ~7 min,
  `backfill_underlying` complete in ~30 min, `backfill_news` at 6/28.
- Wrote `src/sentiment.py` (FinBERT batch scoring) with lazy torch +
  transformers imports so `orchestrator status` stays fast. Per the
  locked `SPEC.finbert_*` fields: `ProsusAI/finbert`, 512-token
  truncation, `P_positive - P_negative` per-article score, input is
  `title + " " + description`. Smoke-tested on 20 Jan-2024 articles
  (~51 s including first-time 440 MB model download); probability
  sums exactly to 1.0, distribution is non-degenerate
  (mean +0.15, std 0.34, range [-0.67, +0.79]), spot-checks read
  sensibly. Scored full Jan-2024 (2 219 articles) on CPU in 6 m 45 s
  (~5 articles/s) and wrote `data/train/sentiment/2024-01.parquet`
  via `storage.write_partitioned_parquet` so the MANIFEST gets a
  sha256 entry alongside the news file.
- Graduated the `score_sentiment` orchestrator stub. The new stage
  walks locked months in order, gates on `backfill_news` having
  marked that month complete, scores only what isn't already on
  disk (so the manual Jan-2024 run carries forward across restarts),
  and treats zero-news months as a no-op completion. FinBERT cache
  in `src.sentiment` module scope means one model load per
  orchestrator process across all months. CPU pace projects to
  ~3 hours for the full 60K-article backfill.
- Operational note: the originally-launched orchestrator hung
  silently mid-`list_ticker_news` for 2024-07 — process alive but
  no log output for ~20 min, last log line at 20:44:05 was the SPY
  ticker pull start. Suspected cause: polygon-api-client's
  paginated generator stalled on a slow response with no client-side
  timeout. Restarting the Scheduled Task picked up 2024-07 cleanly
  (no parquet was written for the hung month). Follow-up: wrap
  `_iter_news_for_ticker` in a per-call timeout so a stuck
  connection becomes a chunk failure instead of a silent stall.

## What I'll do tomorrow

- Audit the overnight orchestrator run via
  `.phase0\orchestrator.log` and `data\MANIFEST.json`: confirm
  `backfill_news` finishes and `score_sentiment` chews through the
  full locked window. Spot-check 2026-04 (last month) sentiment
  distribution against 2024-01 for sanity.
- Wrap `src.news._iter_news_for_ticker` in a per-call timeout so a
  stuck Polygon connection becomes a chunk failure (logged + retried
  on next iteration) instead of a silent stall. This week's hang
  cost ~20 min of wall time before manual restart; that's tolerable
  for a daytime session but unacceptable for an overnight run.
- Wire the `backfill_options` stage end-to-end now that Q1/Q2 are
  settled: daily ATM-open ± 25 pull, sample-construction post-filter
  to ATM-event ± 2, and the `coverage_failures.parquet` writer per
  the new decision-log entry.
- Begin `src/iv_surface.py` (Black-Scholes IV inversion using
  `data/{split}/{options,underlying,risk_free}/*.parquet`) so the
  `compute_iv` orchestrator stub can graduate next.

## Decisions made today

- [Encode the locked pre-registration spec as code](../decisions/2026-05-27_locked-spec-as-code.md)
- [Acquisition-layer architecture and the vendor question](../decisions/2026-05-27_data-acquisition-architecture.md)
- [Eval harness and a resumable laptop-runtime orchestrator](../decisions/2026-05-27_eval-harness-and-orchestrator.md)
- [Q1 (ingest strike window) and Q2 (mid-quote source) resolved with empirical evidence](../decisions/2026-05-27_q1-strike-window-and-q2-mid-quote.md)
